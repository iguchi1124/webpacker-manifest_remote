require "spec_helper"

RSpec.describe Webpacker::ManifestRemote do
  it "has a version number" do
    expect(Webpacker::ManifestRemote::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
